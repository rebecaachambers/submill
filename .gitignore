mihomo/vendor/
vendor/
openipdb.ipdb
config.yaml
test

output/*
submill*

.idea
.vscode
.claude/
.qoder/

.DS_Store

# mihomo binary (compiled into mihomo/), not the config
mihomo/mihomo
!config/mihomo.yaml


# cross-compile output
build/

# Go installers (bundled for offline setup)
# Go installers are distributed via GitHub Releases, not committed to repo
assets/go/
